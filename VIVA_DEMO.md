# Viva demonstration (5 minutes)

1. Run `docker compose up --build` and open port 3000.
2. Explain the four online decoys and detection-only boundary.
3. Click SSH, then HTTP, FTP, and Database demo buttons.
4. Show live counters, event stream, risk changes, alert creation, and attack path.
5. Open `/docs` to show the API and explain the event pipeline.
6. Run `python-backend/scripts/benchmark.py` only if the backend is locally reachable; report measured output, never fabricated values.
7. Explain responsibilities: Rust sensing, Python analysis, React visualization, database persistence.
